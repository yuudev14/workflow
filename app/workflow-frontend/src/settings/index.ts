import BASE_URL from "./urls"
import { FLOW_START_ID, FLOW_SELECT_TRIGGER_ID } from "./reactFlowIds"

const settings = {
  BASE_URL,
  REACT_FLOW_IDS: {
    FLOW_START_ID,
    FLOW_SELECT_TRIGGER_ID
  }

}

export default settings