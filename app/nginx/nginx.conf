events {}

http {
    server {
        listen 9999;
        server_name localhost;

        location / {
            proxy_pass http://host.docker.internal:3000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        location /playbook-api/ {
            proxy_pass http://ytsoar_api:8080/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_redirect http://localhost/ /playbook-api/;
        }

        location /connector-api/ {
            proxy_pass http://ytsoar_api:8080/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_redirect http://localhost/ /connector-api/;
        }

        # Login/refresh/logout/me. The trailing slash strips the prefix, so the
        # api sees /api/auth/v1/*. That prefix strip is also why the refresh
        # cookie is set with Path=/ — a scoped path would never be sent back.
        location /auth-api/ {
            proxy_pass http://ytsoar_api:8080/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_redirect http://localhost/ /auth-api/;
        }

        # websocket upgrade for live playbook status (/ws/playbook). No trailing
        # slash on proxy_pass so the /ws/ path is preserved to the api.
        location /ws/ {
            proxy_pass http://ytsoar_api:8080;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_read_timeout 3600s;
        }
    }
}

# http {
#     server {
#         listen 9999;
#         server_name localhost;

#         location / {
#             proxy_pass http://host.docker.internal:3000;
#             proxy_set_header Host $host;
#             proxy_set_header X-Real-IP $remote_addr;
#         }

#         location /playbook-api/ {
#             proxy_pass http://host.docker.internal:8080/;
#             proxy_set_header Host $host;
#             proxy_set_header X-Real-IP $remote_addr;
#             proxy_redirect http://localhost/ /playbook-api/;
#         }

#         location /connector-api/ {
#             proxy_pass http://host.docker.internal:8000/;
#             proxy_set_header Host $host;
#             proxy_set_header X-Real-IP $remote_addr;
#             proxy_redirect http://localhost/ /connector-api/;
#         }

#         location /ws/ {
#             proxy_pass http://host.docker.internal:8080;
#             proxy_http_version 1.1;
#             proxy_set_header Upgrade $http_upgrade;
#             proxy_set_header Connection "upgrade";
#             proxy_set_header Host $host;
#             proxy_read_timeout 3600s;
#         }
#     }
# }